require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')
  
describe "Connection" do

  context "initializing" do
    xit "loads with server to connect to" do
      
    end
  end
  
  context "connecting" do
    xit "can successfull connect" do
      
    end
    
    xit "fails when no server to connect to" do
      
    end
    
    xit "fails if connection times out" do
      
    end
  end
  
  context "sending data" do
    xit "can send data successfully" do
      
    end
    
    xit "fails when underlying network fails" do
      
    end
    
    xit "fails when command times out" do
      
    end
  end
  
  context "receiving data" do
    xit "can receive data successfully" do
      
    end
    
    xit "fails when underlying network fails" do
      
    end
    
    xit "fails when response times out" do
      
    end
  end
  
  context "sending command" do
    xit "can successfully send command" do
      
    end
    
    xit "fails if unable to connect" do
      
    end
    
    xit "fails if unable to send data" do
      
    end
    
    xit "fails if unable to recieve response" do
      
    end
  end

end
